{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "packages-capability",
  "description": "Capability set for the Packages window. It self-fetches via the app-registered packages_* commands (list_packages, check_package_updates, install_package, update_package, uninstall_package, packages_window_ready) — authorized by core:default (custom commands are not gated by per-command permission identifiers, only plugins are). shell:allow-open lets pack descriptions link out.",
  "windows": ["packages"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open"
  ]
}
