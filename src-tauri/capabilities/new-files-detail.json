{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "new-files-detail",
  "description": "Capability set for the new-files detail window. Only needs event IPC to receive file list data from the main window.",
  "windows": ["new-files-detail"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-focus"
  ]
}
