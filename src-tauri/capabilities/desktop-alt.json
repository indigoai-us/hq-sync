{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-alt-capability",
  "description": "Capability set for the Indigo-only desktop alternate UX window.",
  "windows": ["desktop-alt"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open"
  ]
}
