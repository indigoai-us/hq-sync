{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notification-history",
  "description": "Capability set for the notification-history secondary window. Invokes fetch_notification_history + get_activity_log to load the unified timeline, and open_dm_detail / open_share_detail for per-row click-through. Without this file the window's invoke() calls silently fail the capability check and the window renders empty. Mirrors dm-detail.json.",
  "windows": ["notification-history"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-focus"
  ]
}
